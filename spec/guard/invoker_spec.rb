require "spec_helper"

RSpec.describe Guard::Invoker do
  it "has a version number" do
    expect(Guard::Invoker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
